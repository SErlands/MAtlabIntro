function index = allocate_seat(votes, seats)
% Allocates a seat according to D’Hondt method
% 

f = seats + 1;
r = votes;

z = r./f; % the quotients (jämförelsetalen) of each party

max(z)

n = numel(z); % numeber of elements z

for index = 1:n
    if z(index) == max(z)
        index
    end

end